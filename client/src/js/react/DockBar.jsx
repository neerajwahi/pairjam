/** @jsx React.DOM */
var React = require('react');

var DockBar = React.createClass({
    getInitialState: function() {
        return {
        };
    },

    render: function() {
        return (
            <ul id="dockBar">
            </ul>
        );
    }
});

module.exports = DockBar;
